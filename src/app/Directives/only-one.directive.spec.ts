import { OnlyOneDirective } from './only-one.directive';

describe('OnlyOneDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyOneDirective();
    expect(directive).toBeTruthy();
  });
});
